<p><%= message %></p>
<p>　</p>
<p><a href="/">トップページに戻る</a></p>
<script>
    var COMMAND = 'sudo sh /home/pi/bezelie/testpi/connectWifi.sh';
    exec(COMMAND, function(error, stdout, stderr) {
    // シェル上でコマンドを実行できなかった場合のエラー処理
    if (error !== null) {
        console.log('exec error: ' + error);
        return;
    }
    console.log('connectWifi: ' + stdout);
    }); // end of exec
</script>
